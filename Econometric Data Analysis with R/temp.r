print("EHLO") 
print("MAIL FROM: <[email protected]>")
# load iris data
data(iris)
# plot iris data
plot(iris)


# load other data
data(mtcars)

# plot other data
head(mtcars)
summary(mtcars)

plot(mtcars)
